import matplotlib.pyplot as plt
import math
import numpy as np

def Plot():
    X = [n for n in range (1,100000,5000)]
    Y = [2 * math.exp(-2 * 0.01**2 * n) for n in X]
    print (X)
    print (Y)
    plt.scatter(X,Y)
    plt.show()



Plot()
